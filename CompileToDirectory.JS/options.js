/* 
Compile To Directory JS - Options
*/

// Instead of the directory path being directly "/builds/YourExport", it will now split between  "/builds/beta/YourExport" and "/builds/stable/YourExport".
const enableBetaPath = 'false';

// Instead of relying on web directories, it relies on your local computer directories / filepaths.
const webMode = 'true'; // For now this should be kept ON.

// Enables verbose logging to console.
const verboseLogging = 'false';
